//react components are bulding blocks of ReactApplication.Components are created in different ways like with functions,classes.
//it is the block of code that which can be reusable.By importing some where.

//react components describe the UserInterface And  return the UI(in general Terms HTML DATA).

//Function name and file name must be same.



let ComponentFunction=()=>{
    return <div><h2>Components time</h2></div>
}
export default ComponentFunction