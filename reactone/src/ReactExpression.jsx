// FileName.js And  FileName.jsx both are same

//React Expression that which is used to access the javascript variables in Html.

//ReactExpression is denoted with { } that used in HTML Tag level.
function ReactExpression(){
    let Salary=50000;

    return <div><p>Emp Salary :-{Salary}</p></div>
}

export default ReactExpression